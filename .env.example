# Required only for real AI generation. Never put this value in GitHub.
OPENAI_API_KEY=

# Current economical model used by this demo.
IMAGE_MODEL=gpt-image-2

# Optional: protect the whole temporary demo site with HTTP Basic Auth.
# Username is always: demo
SITE_PASSWORD=

# Keep local temporary job files for this many hours.
JOB_TTL_HOURS=6
